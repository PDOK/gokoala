import { FeatureNamePipe } from './property-value.pipe'

describe('FeatureNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FeatureNamePipe()
    expect(pipe).toBeTruthy()
  })
})
